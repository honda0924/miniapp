.row
  .col.s8.offset-s2
    - @user.liked_blogs.each do |blog|
      .card.teal
        %p
          = blog.content
        .card-action
          Written By:
          =link_to blog.user.name, user_path(blog.user.id)
          = blog.liked_users.count.to_s + "件のいいね"
          =blog.created_at.strftime("%Y-%m-%d %H:%M")
        -if user_signed_in? &&current_user.id == blog.user.id
          .card-action
            = link_to 'Edit', edit_blog_path(blog.id)
            = link_to 'Delete', "blogs/#{blog.id}",method: :delete